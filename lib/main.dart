import 'package:flutter/material.dart';
import 'src/app.dart';
// import 'package:flutter/rendering.dart'; //for using the degubPaintSizeEnabled



void main(){
  // debugPaintSizeEnabled = true; //shows lines of expansion and positioning of stack widget when there are multiple child widgets

  runApp(App());

}